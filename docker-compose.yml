version: '3.8'

services:
  app:
    build: .
    container_name: doctors-app
    restart: always
    network_mode: "host"
    env_file:
      - .env.local
    environment:
      - PORT=3001
      - NODE_ENV=production
      - TZ=Asia/Kolkata
